class Player{
    constructor(x, onHold, onGround){
        this.x = x;
        this.onHold = onHold;
        this.onGround = onGround;
    }
}



function setup(){
    createCanvas(800, 800);
    imageMode(CENTER);
    textAlign(CENTER, CENTER);
}

function draw(){
    background(0);
    push();



    pop();
}
