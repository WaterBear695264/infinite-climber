class Player{
    constructor(x, onHold, onGround){
        this.x = x;
        this.y = y;
    }
}



function setup(){
    createCanvas(800, 800);
    imageMode(CENTER);
    textAlign(CENTER, CENTER);
}

function draw(){
    background(0);
    push();



    pop();
}
